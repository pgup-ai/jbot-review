node_modules/
dist/
*.log
.env
# Any local copy of the env file carries the same live keys.
.env.*
!.env.example
# Worker deploy config — real copy gitignored; commit only the .example
deploy/jbot-review-worker.env.local
.DS_Store
.idea/
.tmp/

# Internal planning/spec docs — local-only, kept out of the public repo
docs/superpowers/

# Local review reports (npm run review:local)
.jbot-review/

# Local observer-gateway journals (npm run gateway)
gateway-data/

# Local CLI-provider auth/state, materialized when dogfooding a provider
.commandcode/
.poolside/

# Local scratch / ad-hoc research notes
.research/
