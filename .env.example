# Required for the hosted webhook App (`npm run dev`). NOT needed for
# `npm run review:local` — local review runs with the provider key alone.
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=

# Provider (defaults to opencode if unset)
# See src/shared/config.ts for all supported providers.
PROVIDER=opencode
OPENCODE_API_KEY=
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
NVIDIA_API_KEY=
ZAI_API_KEY=
XAI_API_KEY=
FIREWORKS_API_KEY=
DEVIN_WINDSURF_API_KEY=
COMMANDCODE_ACCESS_KEY=
CURSOR_API_KEY=
CODEX_AUTH_JSON=
CLINE_AUTH_JSON=
KILO_AUTH_CONTENT=

# Optional (defaults to provider default model)
MODEL=opencode/deepseek-v4-flash-free
PORT=3000

# Local review (`npm run review:local`) — optional knobs. The command loads
# this .env by itself; production entries never read .env.
#JBOT_LOCAL_BASE=origin/main
#JBOT_LOCAL_REPORT=true

# To switch providers, change PROVIDER and the corresponding key:
#   PROVIDER=opencode-go → set OPENCODE_API_KEY
#   PROVIDER=deepseek     → set DEEPSEEK_API_KEY
#   PROVIDER=openai       → set OPENAI_API_KEY
#   PROVIDER=anthropic    → set ANTHROPIC_API_KEY
#   PROVIDER=google       → set GEMINI_API_KEY
#   PROVIDER=openrouter   → set OPENROUTER_API_KEY
#   PROVIDER=nvidia       → set NVIDIA_API_KEY
#   PROVIDER=zai-coding-plan → set ZAI_API_KEY
#   PROVIDER=xai          → set XAI_API_KEY
#   PROVIDER=fireworks-ai → set FIREWORKS_API_KEY
#   PROVIDER=devin        → set DEVIN_WINDSURF_API_KEY
#   PROVIDER=commandcode  → set COMMANDCODE_ACCESS_KEY
#   PROVIDER=cursor       → set CURSOR_API_KEY
#   PROVIDER=codex        → set CODEX_AUTH_JSON
#   PROVIDER=cline / cline-pass → set CLINE_AUTH_JSON
#   PROVIDER=kilo         → set KILO_AUTH_CONTENT
